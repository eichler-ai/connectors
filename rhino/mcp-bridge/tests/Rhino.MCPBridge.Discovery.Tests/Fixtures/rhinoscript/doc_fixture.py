def EnableRedraw(enable=True):
    return enable

def GetObject(message=None, filter=0, preselect=False):
    """Prompts user to pick or select a single object
    Parameters:
      message (str, optional): a prompt or message
      filter (number, optional): the type of object to select
      preselect (bool, optional): allow preselected objects
    """
    return None
