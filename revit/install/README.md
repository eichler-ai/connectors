# Deployment / install tooling

Scripts that install the Revit MCP Bridge into Revit's Addins folder (per Revit version) and register the Revit MCP Server with Claude's MCP client config. See PRD §12 (Signing & distribution) — [`../docs/PRD.md`](../docs/PRD.md).

Not yet scaffolded.
